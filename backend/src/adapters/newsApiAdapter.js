/**
 * STUB — not implemented in this phase.
 * When implemented: will call a news API using config.newsApi.apiKey
 * (see config/env.js), normalize each article into the ContentItem
 * shape, and register itself in services/contentSourcesService.js.
 * The API key never leaves this file.
 */

module.exports = {
  name: "newsApi",
  async fetchItems() {
    return [];
  },
};
