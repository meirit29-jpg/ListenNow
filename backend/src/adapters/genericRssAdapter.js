/**
 * STUB — not implemented in this phase.
 * When implemented: will accept a list of arbitrary RSS feed URLs
 * (websites/blogs that publish a feed), parse and normalize each
 * entry into the ContentItem shape, and register itself in
 * services/contentSourcesService.js.
 */

module.exports = {
  name: "genericRss",
  async fetchItems() {
    return [];
  },
};
