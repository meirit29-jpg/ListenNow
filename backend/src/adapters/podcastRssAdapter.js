/**
 * STUB — not implemented in this phase.
 * When implemented: will fetch and parse one or more podcast RSS feed
 * URLs (no secret required for most public feeds), normalize each
 * <item> into the ContentItem shape, and register itself in
 * services/contentSourcesService.js.
 */

module.exports = {
  name: "podcastRss",
  async fetchItems() {
    return [];
  },
};
